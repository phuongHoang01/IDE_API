version: '2'
services:
  api:
    image: judge0/api:1.3.1
    volumes:
      - ./judge0-api.conf:/judge0-api.conf:ro
    ports:
      - "3000:3000"
    privileged: true
    restart: always

  worker:
    image: judge0/api:1.3.1
    command: ["./scripts/run-workers"]
    volumes:
      - ./judge0-api.conf:/judge0-api.conf:ro
    privileged: true
    restart: always

  db:
    image: postgres:9.6
    env_file: judge0-api.conf
    volumes:
      - C:\Users\zshin\Desktop\ApiComplie\ApiProduction\judge0-api-v1.3.1/srv/postgres-data:/home/postgresql/data/
    restart: always

  redis:
    image: redis:4.0
    command: [
      "bash", "-c",
      'docker-entrypoint.sh --appendonly yes --requirepass "$$REDIS_PASSWORD"'
    ]
    env_file: judge0-api.conf
    volumes:
      - C:\Users\zshin\Desktop\ApiComplie\ApiProduction\judge0-api-v1.3.1/srv/redis-data:/data
    restart: always
